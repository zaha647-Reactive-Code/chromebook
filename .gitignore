# MyChromebook.pk — files that must never go to GitHub
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
# secrets — never commit
.env.local
.env*.local
service-account*.json
*-firebase-adminsdk-*.json
# firebase emulator data
firebase-debug.log
firestore-debug.log
.firebase/
